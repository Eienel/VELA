MONGODB_URI=
GOOGLE_GENERATIVE_AI_API_KEY=
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM
GOOGLE_TTS_VOICE=Kore
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# On-chain token reading (real portfolio data).
# Without these, the app falls back to bundled demo data.
# Alchemy: one key works across Ethereum, Base, Arbitrum — AND Solana.
ALCHEMY_API_KEY=
# Helius (optional): alternative Solana provider. If unset, Solana uses the
# Alchemy key above. Provide either the full RPC URL or the API key.
HELIUS_RPC_URL=
HELIUS_API_KEY=
# Optional: raises CoinGecko rate limits for price + 24h change lookups.
COINGECKO_API_KEY=
# Zerion (free developer plan): best token coverage + DeFi positions
# (staking, LP, lending). When set, it's the preferred portfolio source.
# https://zerion.io/api
ZERION_API_KEY=
